                              # NODE NOTES 

/* what is node ->  Node.js is a JavaScript runtime environment that allows you to run JavaScript outside the browser, mainly on the server. It is built on Chrome's V8 JavaScript engine and provides an event-driven, non-blocking I/O model, making it efficient and suitable for building scalable network applications.
*/

/* why node -> Node.js is popular for several reasons:
 * 1. Performance: Node.js uses the V8 JavaScript engine, which is known for its high performance.
 * 2. Scalability: Node.js is designed to be highly scalable, making it suitable for building          large-scale applications.
 * 3. Simplicity: Node.js provides a simple and intuitive API for building applications.
 */
// 4. Community: Node.js has a large and active community of developers who contribute to its growth and development.

/* where we use node -> Node.js is commonly used for building web applications, APIs, real-time applications (like chat applications), and server-side applications. It is also used for building command-line tools and scripts. */

npm init -y -> This command initializes a new Node.js project and creates a package.json file with default values. The -y flag automatically answers "yes" to all prompts, allowing you to quickly set up your project without having to manually input information.



node js has 3 types of modules ->
1. package.json ->  The package.json file is a crucial part of any Node.js project. It contains metadata about the project, such as its name, version, description, main entry point, scripts, dependencies, and more. This file is used by npm to manage the project's dependencies and scripts.
2. package-lock.json  -. This file is automatically generated when you install dependencies using npm. It contains a detailed description of the exact version of each installed package and its dependencies. The package-lock.json file ensures that the same versions of packages are installed across different environments, providing consistency and stability to your project.
3. node_modules -> This is a directory that contains all the installed dependencies for your Node.js project. When you run npm install, the specified packages and their dependencies are downloaded and stored in the node_modules folder. This allows your project to access and use these packages when needed.  It should not be pushed to version control systems like Git, as it can be easily recreated using the package.json and package-lock.json files.

package.json example ->
{
  "name": "node-app",        // The name of the project
  "version": "1.0.0",            // The version of the project
  "description": "Example Node.js app",        // A brief description of the project
  "main": "index.js",                      // The entry point of the application (the main file to run)
  "scripts": {                         // Scripts that can be run using npm
    "start": "node index.js",         // A script to start the application using Node.js   (ex - npm run start)
    "dev": "nodemon index.js"         
  },
  "author": "Ankit Patel",           // The author of the project
  "license": "MIT",                 // The license under which the project is released
  "dependencies": {                 // The dependencies required for the project
    "express": "^4.18.2"
  },
  "devDependencies": {              // The development dependencies required for the project
    "nodemon": "^3.0.0"
  }
}              -> important in this is  script and dependencies and devDependencies.

npm -> npm stands for Node Package Manager. It is a command-line tool that allows you to manage and install packages (libraries or modules) for your Node.js projects. With npm, you can easily add, update, and remove dependencies for your project, as well as run scripts defined in the package.json file. It is an essential tool for working with Node.js and helps streamline the development process by providing access to a vast ecosystem of packages and tools.

   npm install express -> this install the express package inside the folder and also add it to the dependencies in package.json file.

internal modules -> Node.js has several built-in modules that provide various functionalities, such as file system operations, HTTP server creation, and more. Some commonly used internal modules include:
1. fs: Provides an API for interacting with the file system, allowing you to read, write, and manipulate files and directories.
2. http: Allows you to create HTTP servers and handle HTTP requests and responses.

external modules -> External modules are third-party packages that you can install using npm. These modules are not included in the Node.js core but can be easily added to your project to extend its functionality. Examples of popular external modules include:
1. express: A web application framework for Node.js that simplifies the process of building web applications and APIs.
2. mongoose: An Object Data Modeling (ODM) library for MongoDB and Node.js,


where this  dependencies are used ->   
they are in the package.json file and they are used to specify the packages that your project depends on. When you run npm install, npm reads the dependencies listed in the package.json file and installs the specified packages and their dependencies into the node_modules directory. This allows your project to access and use these packages when needed.
And when you have .gitignore file and you have node_modules in it then when you push your code to github the node_modules folder will not be pushed to github but when someone else clone your code from github and run npm install then the node_modules folder will be created in their local machine with all the dependencies specified in package.json file.

 



                      ## ⚙️ Express JS NOTES 



/* what is express js -> Express.js is a popular web application framework for Node.js. It provides a simple and flexible way to build web applications and APIs. Express.js simplifies the process of handling HTTP requests, routing, middleware, and more, allowing developers to focus on building their applications rather than dealing with low-level details of the Node.js HTTP module.

/* why express js -> Express.js is widely used for several reasons:
1. Simplicity: Express.js provides a simple and intuitive API for building web applications, making it easy for developers to get started quickly.
2. Flexibility: Express.js is unopinionated, meaning it does not impose any specific structure or design patterns on your application. This allows developers to have the freedom to organize their code as they see fit.
3. Middleware: Express.js has a powerful middleware system that allows you to easily add functionality to your application, such as logging, authentication, and more.
4. Community: Express.js has a large and active community of developers who contribute to its growth and development, providing a wealth of resources, plugins, and support for developers using the framework.
*/

``` js 
const express = require("express");   // Importing the Express library to create a web server
const app = express();               // Creating an instance of the Express application
const port = 3000;            // Defining the port number on which the server will listen

app.get("/", (req, res) => {        // Defining a route for the root URL ("/") that sends a "Hello World!" response when accessed
  res.send("Hello World!");           // Sending a response back to the client with the message "Hello World!"
});

app.post("/", function(req, res) {
    res.send("hello world from the post endpoint")
})

app.get("/greet",function(req,res){  // Defining a route for the "/greet" URL that sends a personalized greeting message when accessed
    res.send("hello Ankit")
})

app.listen(port, () => {                        // Starting the server and making it listen on the defined port. The callback function is executed once the server is up and running, logging a message to the console.
  console.log(`Example app listening on port ${port}`);
});

```

query parameters -> Query parameters are a way to pass additional information in the URL of an HTTP request. They are typically used to filter or modify the response from the server. In Express.js, you can access query parameters using the req.query object. For example, if you have a route defined as app.get("/search", (req, res) => { ... }), and you make a request to /search?term=example, you can access the value of the term query parameter using req.query.term, which would return "example". Query parameters are commonly used for search functionality, pagination, and other scenarios where you want to provide additional information to the server without changing the URL structure.
 
 url -> http://localhost:3000/search?name=ankit&age=21
    output -> name = ankit
              age = 21
    req.query -> { name: 'ankit', age: '21' }   it will return an object with key value pair of query parameters.
    app.get("/search", (req, res) => {
  const { name, age } = req.query;

  res.send(`Name: ${name}, Age: ${age}`);
  });

url -> http://localhost:3000/search?n = 5
    output -> n = 5
    req.query -> { n: '5' }   it will return an object with key value pair of query parameters.
    app.get("/search", (req, res) => {
  `const { n } = req.query;   or   const n = req.query.n;`

  res.send(`n: ${n}`);
  });




                # MIDDLEWARES IN EXPRESS JS
-> Middleware functions are functions that have access to the request object (req), the response object (res), and the next middleware function in the application's request-response cycle. They can perform various tasks such as executing code, modifying the request and response objects, ending the request-response cycle, or calling the next middleware function in the stack.
-> Middleware functions are commonly used for tasks such as logging, authentication, error handling, and more. They can be added to the Express application using the app.use() method or by specifying them as arguments in route handlers.
    FOR the example check the folder middleware in the current directory.


fetch API -> The Fetch API is a modern JavaScript API that allows you to make network requests to retrieve resources from a server. It provides a more powerful and flexible way to handle HTTP requests and responses compared to the older XMLHttpRequest API. The Fetch API uses Promises, making it easier to work with asynchronous operations and handle responses in a more readable and maintainable way. You can use the fetch() function to make GET, POST, PUT, DELETE, and other types of HTTP requests, and it returns a Promise that resolves to the Response object representing the response from the server.
   for this refer fetchAPI 

to convert the string into the json -> let x = '{"name" : "Ankit"}'
                                        let y = JSON.parse(x);
                                  This is the way to convert the string into the json file then only we will be able to do( y.name)    but not (x.name)  because the type of x is string 
                                    
                                  JSON.stringify({}) -> used to convert the object into the string 
                        



/*

    // NOTES  ON Route Trafficking


    ## Route Trafficking (Traffic Routing)

**Route trafficking** (also called **traffic routing**) is the process of directing incoming network or HTTP requests to the correct destination, such as a specific route, function, or server.

---

### 🔑 Basic Concept

When a user sends a request (e.g., visiting a website or calling an API), the system must decide:

> “Where should this request go?”

This decision-making process is called **routing traffic**.

---

### 🌐 URL Structure and Routing

A URL consists of two important parts:

1. **Path (Used for Routing)**

   * Example: `/users`
   * Determines which route handler or function will be executed.

2. **Query Parameters (Used for Data)**

   * Example: `?id=10&role=admin`
   * Provides additional data to the selected route.

👉 **Important:**
Routing is done based on the **path**, not the query parameters.

---

### 🚀 Example (Express.js)

```js
app.get("/users", (req, res) => {
    console.log(req.query);
    res.send("Users route");
});
```

Request:

```
/users?id=10
```

* `/users` → decides the route
* `id=10` → extra data used inside the route

---

### 🔀 Types of Route Trafficking

1. **Application-Level Routing**

   * Done inside frameworks like Express.js
   * Example: `/home`, `/profile`

2. **Load Balancing**

   * Distributes traffic across multiple servers
   * Prevents server overload

3. **Geo-Based Routing**

   * Sends requests to the nearest server based on location

4. **A/B Testing Routing**

   * Directs users to different versions of a service

5. **Proxy/CDN Routing**

   * Tools like Nginx or Cloudflare route traffic between services

---

### 📦 Types of Request Data

* **Path Parameters:** `/users/10` → `req.params`
* **Query Parameters:** `/users?id=10` → `req.query`
* **Request Body:** Used in POST/PUT requests → `req.body`

---

### 🧠 Summary

* Route trafficking = directing requests to the correct destination
* Routing is mainly based on the **URL path**
* Query parameters are only used to pass additional data
* Can happen at multiple levels: application, server, or global network

---


## Use of Middleware, Nginx, and Cloudflare in Traffic Routing

Traffic routing can happen at different levels in a system. The three common layers are **Middleware (Application Level)**, **Nginx (Server Level)**, and **Cloudflare/CDN (Global Level)**.

---

### 1. 🧩 Middleware (Application Level)

**Use:**

* Handles routing inside your backend application (e.g., Express.js)
* Decides which function or controller should process the request

**Key Responsibilities:**

* Route handling (`/users`, `/login`)  also can modify it.
* Authentication & authorization        can end the loop cycle like if there is no authentication
* Rate limiting
* Logging and request validation
 have 2 works 
 1. it may or may not change the request object
 2. it will either stop the request right there  or forward the request to the real handlers 

**Example:**

```js
app.get("/users", (req, res) => {
    res.send("Users data");
});
```

👉 Used when request has already reached your backend server

---

### 2. ⚙️ Nginx (Server Level)

**Use:**

* Acts as a **reverse proxy** and **load balancer**
* Routes traffic between multiple backend servers

**Key Responsibilities:**

* Load balancing (distribute traffic)
* Reverse proxy (forward requests to backend)
* SSL termination (HTTPS handling)
* Static file serving

**Example Use Case:**

* `/api` → Node.js server
* `/images` → static file server

👉 Used when you have **multiple servers or services**

---

### 3. 🌍 Cloudflare / CDN (Global Level)



| Level        | Tool       |
| ------------ | ---------- |
| App level    | Express    |
| Server level | Nginx      |
| Global level | Cloudflare |


Commonly used middleware 
 1. -> express.json()
       express.json() is a built-in middleware in Express.js used to parse incoming JSON data from HTTP requests.
   🔹 What it does

  When a client (frontend, Postman, etc.) sends data in JSON format, it is received as a raw stream.
  express.json() converts that raw JSON into a JavaScript object and puts it inside:
👉 req.body


const express = require("express");

cosnt app = express();

app.use(express.json());                 // middleware added       it internally use the body-parser  

app.post("/data" , (req, res) => {
   console.log(req.body);
   res.send("Data recieved");
})

2. CORS (cross origin resource sharing) 
   -> CORS is a security mechanism used by browsers to control how resources (APIs, data) are shared between different origins.
   -> BY DEFAULT cross origin request are blocked 
   


